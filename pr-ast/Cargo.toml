[package]
name = "pr-ast"
version.workspace = true
edition.workspace = true

[dependencies]
pr-common = { path = "../pr-common" }
pr-tokenize = { path = "../pr-tokenize" }

lsp-types = { workspace = true }
